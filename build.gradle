repositories {
    mavenCentral()
}

apply plugin: "java"

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
}

sourceSets {
    main.java.srcDir 'com.AMPro.Weebot.main'
}

jar {
    manifest.attributes "Main-Class": "com.AMPro.Weebot.main.Launcher"
}