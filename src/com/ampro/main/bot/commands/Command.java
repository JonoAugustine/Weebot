/**
 *
 */

package com.ampro.main.bot.commands;

/**
 * A {@code Command}
 * ? Not sure if this is actually the way-to-go.
 * ? The way i've seen it implemented, this is basically just another listner
 */
public class Command {

}
